# Tasks: Circle Arc Runtime Integration

## Review Workload Forecast

| Field | Value |
|-------|-------|
| Estimated changed lines | ~700–850 |
| 400-line budget risk | High |
| Chained PRs recommended | Yes |
| Suggested split | PR 1 (batch 1 provider hardening + batch 2 seam) → PR 2 (batch 3 policy parity + batch 4 receipt/explorer) → PR 3 (batch 5 health + batch 6 boot/env/docs) → PR 4 (batch 7 verification + fake-Circle integration + runbook execution) |
| Delivery strategy | ask-on-risk |
| Chain strategy | pending |

```text
Decision needed before apply: Yes
Chained PRs recommended: Yes
Chain strategy: pending
400-line budget risk: High
```

Rationale: the change is a wiring-plus-hardening bridge over a rescued provider. It touches the provider (`src/wallet/circle-arc-provider.ts`: hardened `waitForFinality` + chainId pre-check + receipt-hash echo), two policy gates (`src/agent/definition.ts`, `src/agent/wallet-agent.ts`), the idempotency seam (three construction sites: `buildGuardedTools`, `toTransferRequest`, `sendToken`), the confirm-branch provider routing and receipt-waiter selection, the explorer URL normalization, the additive `/health` provider field, the D8 boot guard, `.env.example`, docs, and a new fake-Circle integration test — plus extensions to the provider, policy, health, and seam unit suites. Because it crosses the provider seam and the text/voice confirmation flow, the slices are independently revertible (provider + seam reverted without touching the policy gate or health contract), which keeps review diffs focused.

### Suggested Work Units

| Unit | Bounded outcome | Focused test | Runtime harness | Rollback boundary |
| --- | --- | --- | --- | --- |
| 1 | Provider hardening + previewId seam (batches 1–2) | `npm test -- --run tests/unit/circle-arc-provider.test.ts tests/unit/wallet-agent-guard.test.ts` | none (injected fake client/rpc/clock) | `src/wallet/circle-arc-provider.ts`, `src/agent/definition.ts`, `src/agent/wallet-agent.ts`, `src/conversations/service.ts` |
| 2 | Policy parity + receipt/explorer (batches 3–4) | `npm test -- --run tests/unit/wallet-agent-definition.test.ts tests/unit/wallet-agent-policy.test.ts` | none (env-pinned) | `src/agent/definition.ts`, `src/agent/wallet-agent.ts`, `src/wdk/transaction-receipt.ts` (byte-identical) |
| 3 | Health + boot guard + env/docs (batches 5–6) | `npm test -- --run tests/unit/health.test.ts` (or the existing health suite) | `docker compose config` check | `src/contracts/http.ts`, `src/api/health.ts`, `src/runtime/dependencies.ts`, `.env.example`, docs |
| 4 | Verification + fake-Circle integration + runbook execution (batch 7) | `npm run typecheck && npm run lint && npm test` | fake Circle transport + in-memory repository; manual real-cred E2E box left unchecked | `tests/integration/fake-circle-transfers.test.ts`, runbook |

Note: `strict_tdd: false` (Standard Mode). Tests are colocated under `tests/`; no RED/GREEN ceremony — write the code then the focused Vitest coverage, or add coverage alongside the change per batch.

## Batch 1: Provider Hardening (`src/wallet/circle-arc-provider.ts`)

- [ ] 1.1 In `CircleArcProvider.waitForFinality`, before any polling, issue a single `eth_chainId` RPC call and assert the parsed chain id equals `ARC_TESTNET_CHAIN_ID` (5042002n / `0x4ce0d2`). On a parse failure or a mismatch, throw an error naming `arc-testnet`'s chain id as the only supported chain, so a receipt from a different chain can never confirm. Reuse the existing `ARC_TESTNET_CHAIN_ID` constant; do not hardcode the hex literal separately from it. Depends: none. **CAR-008, CAR-009, CAR-014**. <!-- sdd-owner: implementation -->
- [ ] 1.2 In `CircleArcProvider.waitForFinality`, after each `eth_getTransactionReceipt`, validate that the returned receipt echoes the requested `transactionHash` (case-insensitive). A receipt whose `transactionHash` does not match throws `receipt for a different transaction`; only then interpret `0x1` → `confirmed` and `0x0` → `reverted`. Keep missing-receipt and transient RPC errors as keep-polling to the 120 s deadline, then throw (never a fabricated `confirmed`). Depends: 1.1. **CAR-008, CAR-009**. <!-- sdd-owner: implementation -->
- [ ] 1.3 Extend `tests/unit/circle-arc-provider.test.ts` for the hardened finality path: (a) `eth_chainId` returning a non-arc-testnet value → the wait rejects and never returns `confirmed`/`reverted`; (b) a receipt whose `transactionHash` does not echo the requested hash → rejects with `different transaction`; (c) a `0x1` receipt that echoes the hash → `confirmed` with the requested hash/network; (d) `0x0` → `reverted`. Extend the existing `fakeRpc(state)`/`FakeState` fixture so `eth_getTransactionReceipt` can return a `transactionHash` for the echo check. Depends: 1.1, 1.2. **CAR-008, CAR-009, CAR-014**. <!-- sdd-owner: implementation -->
- [ ] 1.4 Add an idempotency unit test in `tests/unit/circle-arc-provider.test.ts`: with a `TransferRequest` carrying `previewId: P`, assert `broadcastTransfer` sends `idempotencyKey` and `refId` equal to `P` to `client.createTransaction` (capture the input via the fake `createTransaction`); a direct call with no `previewId` uses a generated non-empty key as the last resort. Assert the previewed path never falls back to a freshly generated key. Depends: none (provider already carries `request.previewId ?? randomUUID()`). **CAR-006**. <!-- sdd-owner: implementation -->
- [ ] 1.5 Pin the testnet-only and honest-history invariants in `tests/unit/circle-arc-provider.test.ts`: any non-`arc-testnet` read/transfer (`assertNetwork`) rejects naming `arc-testnet`; `listNetworks` returns only `[{ network:'arc-testnet', kind:'testnet' }]`; `listTokens` returns only `[{ network:'arc-testnet', token:'USDC', decimals:18 }]`; `getHistory` returns an empty `transactions` array. Existing coverage is present for some of these — extend where missing and keep the full suite green. Depends: none. **CAR-011, CAR-014**. <!-- sdd-owner: implementation -->
- [ ] 1.6 Keep the provider's no-silent-success mapping pinned by test: a Circle `400`/`code:2` parameter rejection → `not_dispatched`; a transient/SDK error or a dispatch-result timeout → `uncertain` (with a `Do not re-broadcast`-style reason); an unusable/over-cap fee in `previewTransfer` → throw. No path may return a fixture-style unconditional `submitted`. Depends: none. **CAR-003, CAR-008, CAR-009**. <!-- sdd-owner: implementation -->

## Batch 2: Seam Fixes (previewId transport + provider-routed confirm)

- [ ] 2.1 In `src/agent/definition.ts`, add `export type SendTokenBroadcastInput = SendTokenInput & { previewId?: string }` and change the internal `sendToken(input, context)` to accept `SendTokenBroadcastInput`. When `input.previewId` is present, copy it into the outgoing `TransferRequest`. Leave `sendTokenInputSchema`/`SendTokenInput` unchanged (the model never sees `previewId`; no idempotency-key injection surface). Depends: none. **CAR-006, CAR-016**. <!-- sdd-owner: implementation -->
- [ ] 2.2 In `src/agent/wallet-agent.ts` `buildGuardedTools.guardedSendToken`, after the `dryRun:false && !pendingMatches(...)` `confirmation_required` guard passes, inject `previewId: session.pendingTransfer.previewId` into the call input passed to `baseSendToken.execute`; keep the `dryRun:true` path input unchanged. This is the single injection point covering both text and tool confirm flows. Depends: 2.1. **CAR-006, CAR-007**. <!-- sdd-owner: implementation -->
- [ ] 2.3 In `src/conversations/service.ts` `toTransferRequest`, forward `transfer.previewId` into the returned `TransferRequest` using `...(transfer.previewId ? { previewId: transfer.previewId } : {})`. Depends: none. **CAR-006**. <!-- sdd-owner: implementation -->
- [ ] 2.4 In `src/agent/wallet-agent.ts`, the confirm branch of `handleMessage` (the `session.pendingTransfer && CONFIRM_PHRASES.has(normalized)` block) currently calls `await getWdkTools()` unconditionally. Change it to build the confirm-path tools from `createWalletAgentDefinition()` + `toAiSdkTools(...)` when `options.walletProvider` is present (same construction the non-confirm branch already uses), and fall back to `getWdkTools()` only when no provider is supplied (legacy WDK callers). Pass `options.walletProvider` into `executeConfirmedTransfer`. Depends: none. **CAR-013**. <!-- sdd-owner: implementation -->
- [ ] 2.5 In `src/agent/wallet-agent.ts` `executeConfirmedTransfer`, widen the signature to accept `walletProvider?: WalletProvider` (in addition to the existing `transactionReceiptWaiter`/`abortSignal`) and replace the three hardcoded `Sepolia`-literal receipt/error strings ("The Sepolia receipt could not be verified.", "The Sepolia receipt is invalid.", "The Sepolia receipt does not match the transfer.", and the reverted message) with text parameterized by `pending.network` (e.g. "on {network}"). On the reverted path, use the network name instead of `Sepolia`. Depends: 2.4. **CAR-008, CAR-010, CAR-013**. <!-- sdd-owner: implementation -->
- [ ] 2.6 Add confirm-path fail-closed unit tests (in `tests/unit/wallet-agent-guard.test.ts` or a colocated seam suite): duplicate confirm while `broadcasting` → `broadcast_in_progress`; retry after an `uncertain` outcome → `broadcast_uncertain` and no second broadcast; confirm on a missing/superseded preview → `stale_preview`/no broadcast; and the injected `previewId` is present on the broadcast call input for a confirmed preview. Depends: 2.2, 2.4. **CAR-006, CAR-007**. <!-- sdd-owner: implementation -->

## Batch 3: Policy Parity (shared `isLiveTransferSource`)

- [ ] 3.1 In `src/agent/definition.ts`, add `export function isLiveTransferSource(environment: NodeJS.ProcessEnv = process.env): boolean { return environment.WDK_TOOLS_SOURCE === 'live' || environment.WDK_TOOLS_SOURCE === 'circle-arc'; }`. Depends: none. **CAR-004**. <!-- sdd-owner: implementation -->
- [ ] 3.2 In `src/agent/definition.ts` `validateWalletTransferPolicy`, replace `if (process.env.WDK_TOOLS_SOURCE !== 'live') return undefined;` with `if (!isLiveTransferSource()) return undefined;`. Keep all existing invariants and message shapes. Depends: 3.1. **CAR-004, CAR-005**. <!-- sdd-owner: implementation -->
- [ ] 3.3 In `src/agent/wallet-agent.ts` `validateLiveTransferPolicy`, replace `if (process.env.WDK_TOOLS_SOURCE !== 'live') return null;` with `if (!isLiveTransferSource()) return null;` and import `isLiveTransferSource` from `./definition.js`. Keep all existing invariants and message shapes. Depends: 3.1. **CAR-004, CAR-005**. <!-- sdd-owner: implementation -->
- [ ] 3.4 Add fail-closed drift tests for BOTH gates in `tests/unit/wallet-agent-definition.test.ts` (`validateWalletTransferPolicy`) and `tests/unit/wallet-agent-policy.test.ts` (`validateLiveTransferPolicy`): with `WDK_TOOLS_SOURCE=circle-arc` and (a) no `WDK_MAX_TRANSFER_AMOUNT`/`WDK_ALLOWED_RECIPIENTS` → `policy_rejected`; (b) over-limit amount → `policy_rejected`; (c) non-allowlisted/burn/self recipient → `policy_rejected`; (d) wallet/network/token mismatch → `policy_rejected`; (e) a matching transfer → passes. Assert `WDK_TOOLS_SOURCE=live` behavior is byte-identical (pre-existing cases still reject/pass identically) so a drift at one gate cannot reintroduce a circle-arc bypass. Depends: 3.2, 3.3. **CAR-004, CAR-005**. <!-- sdd-owner: implementation -->

## Batch 4: Receipt Verification + Explorer URL

- [ ] 4.1 In `src/agent/wallet-agent.ts` `executeConfirmedTransfer`, select the finality waiter from the provider: when `walletProvider` is present, verify via `(tx, opts) => walletProvider.waitForFinality({ transaction: tx, ...opts })`; otherwise use `defaultTransactionReceiptWaiter`. This makes the provider's `waitForFinality` the single Arc verification source (D3). Do NOT branch `src/wdk/transaction-receipt.ts` on `arc-testnet` or on `WDK_TOOLS_SOURCE` — `defaultTransactionReceiptWaiter` stays byte-identical. Depends: 2.5. **CAR-008, CAR-009**. <!-- sdd-owner: implementation -->
- [ ] 4.2 In `src/agent/definition.ts` `normalizeBroadcastResult`, import `explorerUrlFor` from `../wallet/provider.js` and replace the hardcoded `https://sepolia.etherscan.io/tx/${hash}` with `explorerUrlFor(network, hash)`. Sepolia behavior must remain unchanged (`explorerUrlFor('sepolia', hash)` already returns the etherscan URL). Depends: none. **CAR-010**. <!-- sdd-owner: implementation -->
- [ ] 4.3 Add explorer and receipt-waiter unit tests: (a) in `tests/unit/wallet-agent-definition.test.ts`, `normalizeBroadcastResult` on `arc-testnet` → `https://testnet.arcscan.app/tx/{hash}` and on `sepolia` → the sepolia etherscan URL; (b) a receipt-waiter-selection test asserting `executeConfirmedTransfer` with a `walletProvider.waitForFinality` maps a `0x0` receipt → `transfer_reverted` and a thrown/deadline verification → `transaction_receipt_invalid` (never a fixture 'confirmed'), while a provider-absent call keeps the `defaultTransactionReceiptWaiter` behavior. Depends: 4.1, 4.2. **CAR-008, CAR-010**. <!-- sdd-owner: implementation -->

## Batch 5: Health Contract (additive provider field)

- [ ] 5.1 In `src/contracts/http.ts` `healthResponseSchema`, add `provider: z.object({ status: z.enum(['healthy','degraded','unavailable']), reason: z.string().optional() }).optional()`. This is the only `/v1`-adjacent contract change; no `/v1` request/response shape changes (CAR-016). Depends: none. **CAR-012, CAR-016**. <!-- sdd-owner: implementation -->
- [ ] 5.2 In `src/api/health.ts` `GET /health`, call `await dependencies.wallet.health({ wallet: WALLET(), network: NETWORK() })` and include the additive `provider` field in the response. Preserve the existing `status`/`mode`/`mcp`/`wallet`/`network` semantics (`MODE()` already maps `circle-arc` → `'live'`; `NETWORK()` reads `WDK_NETWORK`) and the lazy env reads. No new endpoint. Depends: 5.1. **CAR-012, CAR-016**. <!-- sdd-owner: implementation -->
- [ ] 5.3 Add health unit tests (in `tests/unit/` beside the health or provider suite): a healthy provider → `provider.status === 'healthy'` with no `reason`; an unhealthy provider (Circle API / Arc RPC down) → `provider.status === 'unavailable'` with a `reason` that contains NO `CIRCLE_API_KEY`, `CIRCLE_ENTITY_SECRET`, or `CIRCLE_SENDER_WALLET_ID` value (credential-scrub assertion, CAR-017); under circle-arc, `mode === 'live'` and `network === 'arc-testnet'`. Depends: 5.2. **CAR-012, CAR-017**. <!-- sdd-owner: implementation -->
- [ ] 5.4 Frontend mirror check: verify `apps/nana-wallet/src/lib/api-types.ts` has no health response type to mirror today (its `provider`/`providerLogoUrl` fields are token metadata, not health) and that no `/v1` request/response shape changed. If a consumer later adds a health type, it must mark `provider?` optional and ignore unknown JSON fields. No source change is expected unless a health type is discovered. Depends: 5.1. **CAR-016**. <!-- sdd-owner: implementation -->

## Batch 6: Boot Guard (testnet-only), `.env.example`, compose, and docs

- [ ] 6.1 In `src/runtime/dependencies.ts` `createWalletProvider`, inside the `WDK_TOOLS_SOURCE === 'circle-arc'` branch, add the D8 config-shape guard: a set-but-mismatched `WDK_NETWORK` (not `arc-testnet`) or `WDK_TOKEN` (not `USDC`) throws `CircleArcConfigError` at boot (import `CircleArcConfigError` from `../wallet/circle-arc-provider.js`). The provider's `ARC_TESTNET_NETWORK`/`TOKEN` constants stay module-level and `rpcUrl` is only overridable via injected test options; use the exported `ARC_TESTNET_NETWORK` constant rather than a new literal. Depends: none. **CAR-002, CAR-014**. <!-- sdd-owner: implementation -->
- [ ] 6.2 Add a boot-guard unit test in `tests/unit/circle-arc-provider.test.ts`: `createWalletProvider({ WDK_TOOLS_SOURCE:'circle-arc', WDK_NETWORK:'sepolia', ... })` → throws `CircleArcConfigError`; a `WDK_TOKEN:'USDT'` variant → throws; `WDK_NETWORK:'arc-testnet'` + `WDK_TOKEN:'USDC'` → returns a `circle-arc`/`live` provider. Depends: 6.1. **CAR-002, CAR-014**. <!-- sdd-owner: implementation -->
- [ ] 6.3 Finalize `.env.example`: complete the rescued circle-arc section — `CIRCLE_API_KEY`, `CIRCLE_ENTITY_SECRET` (comment that it must be 64 hex chars), `CIRCLE_SENDER_WALLET_ID`; for circle-arc set `WDK_NETWORK=arc-testnet` and `WDK_TOKEN=USDC`; document the mandatory live-mode policy vars `WDK_MAX_TRANSFER_AMOUNT` and `WDK_ALLOWED_RECIPIENTS` (fail closed, `policy_rejected`); and state the trust model: Circle holds the wallet keys server-side under the developer entity, the backend holds the entity secret as a production-grade secret (never logged, never sent to the client), the user's device never signs, and Arc TESTNET only. Depends: none. **CAR-014, CAR-017**. <!-- sdd-owner: implementation -->
- [ ] 6.4 Compose verification: confirm `compose.yaml` pins no wallet vars (no `WDK_*`/`SEPOLIA`/`CIRCLE`/`ARC` keys) and that `.env` interpolation drives `WDK_TOOLS_SOURCE=circle-arc` without any compose-level sepolia/network pinning. Assert NO `compose.yaml` change is required (run `docker compose config` if available) and record the result in the runbook/docs note. Depends: none. **CAR-014, CAR-016**. <!-- sdd-owner: implementation -->
- [ ] 6.5 Add an "Arc Testnet (Circle developer-controlled wallet)" section to `docs/local-live-runbook.md` (or a new `docs/arc-testnet-runbook.md`): document the `WDK_TOOLS_SOURCE=circle-arc` env setup (creds + `WDK_NETWORK=arc-testnet` + `WDK_TOKEN=USDC` + policy vars), the trust model (server-side Circle keys, device never signs), the testnet-only boundary (chainId 5042002, USDC only, no mainnet config), the `/health` additive `provider` field and `mode: live`/`network: arc-testnet` behavior, and the comment that `docs/api.md` is left untouched (no `/v1` contract change). Depends: 6.3. **CAR-012, CAR-014, CAR-017**. <!-- sdd-owner: implementation -->

## Batch 7: Verification, Integration, and Manual E2E

- [ ] 7.1 Root verification gates: `npm run typecheck` and `npm run lint` exit 0; the full `npm test` (Vitest) suite is green (369+ baseline tests plus the new/extended circle-arc, policy-parity, health, seam, and explorer tests). Confirm no regression on the `fixture`/`live`/WDK paths and that `tests/unit/circle-arc-provider.test.ts` extensions pass. Depends: batches 1–3. **CAR-004..CAR-017** (broad). <!-- sdd-owner: implementation -->
- [ ] 7.2 Frontend suite: run the frontend typecheck/test suite as-is (no source changes expected); confirm `apps/nana-wallet` still typechecks/builds and that no `/v1` contract shape changed (CAR-016). Do not modify frontend source unless a Sepolia/USDT hardcode is discovered in a fixture/test. Depends: 5.4. **CAR-016**. <!-- sdd-owner: implementation -->
- [ ] 7.3 Add the fake-Circle integration test for the typed flow, e.g. `tests/integration/fake-circle-transfers.test.ts`: drive `createWalletConversationService` with a real `CircleArcProvider` over injected fake `client` + `rpc` + an in-memory repository — preview → confirm → broadcast → finality happy path; Circle API failure mid-broadcast → `broadcast_uncertain` and the session is blocked; a retry after `uncertain` → `broadcast_uncertain` with no second `createTransaction`; a reverted receipt → `transfer_reverted`; and `previewId` reuse verified through `runFinancialTransfer` (via `toTransferRequest`). Depends: 2.3, 2.2, 4.1. **CAR-006, CAR-007, CAR-008, CAR-009**. <!-- sdd-owner: implementation -->
- [ ] 7.4 Manual E2E runbook execution (real `CIRCLE_*` creds, real testnet USDC, explicit user consent) — document the numbered steps in the runbook from 6.5 and leave this box intentionally UNCHECKED for the user. Steps: boot `WDK_TOOLS_SOURCE=circle-arc` with real creds + policy vars + `WDK_NETWORK=arc-testnet`/`WDK_TOKEN=USDC`; `curl /health` → `mode: live`, `network: arc-testnet`, `provider.status: healthy`; voice balance (real Arc RPC USDC) then a preview→confirm below `WDK_MAX_TRANSFER_AMOUNT` to an allowlisted recipient, confirming narration never claims "sent" before the on-chain receipt; text preview→confirm and verify the Confirm/Cancel card links to `testnet.arcscan.app/tx/{hash}`; negative over-limit → `policy_rejected` and non-allowlisted → `policy_rejected`; confirm the Arcscan transaction shows a real USDC transfer and the demo stays testnet-only. Depends: 6.5. **CAR-001..CAR-017** (manual). <!-- sdd-owner: implementation -->

## Post-Apply Review

- [ ] Start or reuse a bounded review of the candidate after the apply phase completes (frozen candidate, at-most-one correction, terminal receipt), covering the provider hardening, the idempotency seam, both policy gates, receipt-waiter selection, explorer URL, the additive health field, the D8 boot guard, `.env.example`/docs, and the fake-Circle integration test slices. <!-- sdd-owner: parent -->
